"use strict"  // treat all js code as newer version of js

// alert("hello", 3+5); we are using nodejs not browser

let name="kunal"
let age=20
let isLoggedIn=false
let state=null

console.log(state);
// datatypes:

// number => range upto 2^53
// biginit

// string=> ""

// boolean : true or false

// null: standalone value (representation of empty value)

// undefined : variable declared but not given value

// symbol : uniqueness (used mainly in react)

// object 

console.log(typeof "kunal");

console.log(typeof null);  // null type is object

console.log(typeof undefined);  // undefined type is undefined